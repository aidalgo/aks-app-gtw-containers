---
apiVersion: v1
kind: Namespace
metadata:
  name: test-app-ingress
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: echo-server
  namespace: test-app-ingress
spec:
  replicas: 2
  selector:
    matchLabels:
      app: echo-server
  template:
    metadata:
      labels:
        app: echo-server
    spec:
      containers:
        - name: echo-server
          image: ealen/echo-server:latest
          ports:
            - containerPort: 80
          env:
            - name: PORT
              value: "80"
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "128Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: echo-server
  namespace: test-app-ingress
spec:
  selector:
    app: echo-server
  ports:
    - port: 80
      targetPort: 80
  type: ClusterIP
---
# Standard Kubernetes Ingress — no Gateway API objects needed.
# Reuses the ApplicationLoadBalancer CR from the test-app namespace so both
# demos share one AGC traffic controller (and one subnet association).
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: echo-ingress
  namespace: test-app-ingress
  annotations:
    alb.networking.azure.io/alb-name: alb
    alb.networking.azure.io/alb-namespace: test-app
spec:
  ingressClassName: azure-alb-external
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: echo-server
                port:
                  number: 80
